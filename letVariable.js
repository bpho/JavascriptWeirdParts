if (a > b) {
	// let is only available in this block code during this period of time
	let c = true;
}

/*
* If you use let in a for loop, every time it's looped you are using a different
* variable in memory. This allows for block-scoping
*/